# Bulk Tag Update

This endpoint is documented at [`../tags/bulk-update.md`](../tags/bulk-update.md).

See that file for full documentation of `POST /contacts/bulk/tags/update/{type}`.

This file exists for discoverability — bulk tag operations live under both `bulk/` and `tags/` subdomains.
